# Mandari Django Requirements
# Generated from pyproject.toml for Docker builds

# Django 6.0
django>=6.0
django-htmx>=1.19.0
django-unfold>=0.40.0
whitenoise>=6.7.0

# Database
psycopg[binary]>=3.2.0
dj-database-url>=2.2.0

# Cache & Sessions
redis>=5.2.0
django-redis>=5.4.0

# Environment
python-dotenv>=1.0.0

# Search
meilisearch>=0.31.0

# AI
groq>=0.11.0
openai>=1.0.0
httpx>=0.28.0

# Utilities
python-dateutil>=2.9.0
pillow>=10.3.0  # Security: fixes heap buffer overflow, DoS, eval injection
markdown>=3.7
cryptography>=44.0.0

# Document extraction (OCR)
# Note: PyPDF2 is deprecated, using pypdf instead
pypdf>=6.4.0  # Security: fixes resource exhaustion, data amplification, infinite loop
pytesseract>=0.3.10
pdf2image>=1.16.0

# PDF generation
xhtml2pdf>=0.2.17  # Security: fixes ReDoS vulnerability
reportlab>=4.0.0

# Production server
gunicorn>=23.0.0

# Transitive dependencies pinned for security
urllib3>=2.5.0  # Security: fixes open redirect, info disclosure
requests>=2.32.4  # Security: fixes control flow, sensitive data issues
anyio>=4.4.0  # Security: fixes race condition
zipp>=3.19.1  # Security: fixes infinite loop
